# SPDX-FileCopyrightText: 2026 ursa.nz <code@ursa.nz>
# SPDX-License-Identifier: GPL-3.0-or-later

# Pinned so every build of the engine reproduces, on this machine and in CI. Bump deliberately,
# with a green conformance run.
[toolchain]
channel = "1.96.0"
